export const concat = (a, b) => a.toString() + b.toString();

export const sum = (arr, inputs) => {
    const num = [];
    // console.log(inputs)
    // console.log(arr)
    // for (let key of inputs) {
    //     arr.forEach(item => {
    //         if (item === key.toString()) {
    //             console.log(item);
    //             console.log(typeof key);
    //             num.push(inputs[key]);
    //         }
    //     })
    // }

    return num;
};